<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <system.web>
    <!-- TODO: set debug="false" in release version -->
    <compilation debug="true">
      <assemblies>
        <add assembly="*" />
      </assemblies>
    </compilation>
    <customErrors mode="Off" />
  </system.web>
</configuration>
